Hey, I need to make a 2D graphics editor in C for my course project. The canvas is basically an 80x24 grid (a 2D char array) where empty space is _ and drawn lines/shapes are *. The user should be able to draw lines, circles, rectangles, and triangles. The tricky part is deleting or modifying shapes after they are drawn, since their pixels merge on the screen. How should we set up the structures and files for this so we can keep track of each shape individually?
That makes sense, keeping a list of active shapes is a good idea. Let's start coding. Can you help me write the core canvas code and the drawing algorithms? I need functions for lines, circles, rectangles, and triangles. Let's use Bresenham's algorithm for the lines and circles so they render cleanly. Also, can we display coordinate numbers on the top and left borders of the grid so it's easy to see where we are drawing?
 Now we need the code to manage the list of shapes. We need to be able to add a new shape, delete one by its ID, and modify an existing one. Whenever a shape is deleted or modified, we need to clear the canvas grid and redraw all the remaining shapes from the list. How should we write the code for this?
 I want to add colors to the menu options and draw the shapes in green using ANSI escape codes. But I know Windows CMD sometimes needs configuration to support ANSI. Also, can we add some helper functions to validate inputs? I don't want the program to crash if a user enters a letter instead of a number or types coordinates that are out-of-bounds.
We need to show the canvas grid, print the active shape list with their IDs underneath it, and then display a menu with options to add shapes, delete shapes, modify shapes, clear the canvas, or exit. Also, make sure that when a user adds a shape, the coordinates are locked within the canvas limits so they don't break the array boundaries."
I want to add a save and load feature to make the project stand out. It should prompt the user for a filename, write the active shape list coordinates to a text file, and be able to read them back to restore the drawing. How can we implement this using standard C file functions?
I tried compiling the project using the MSVC compiler on Windows. The compiler successfully builds the executable, but it's giving me standard warnings saying functions like fscanf and fopen are unsafe. How can we suppress these warnings in our compile command without breaking standard C portability?